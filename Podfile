platform :ios, '9.0'
use_frameworks!

target 'Straal' do
	pod 'IDZSwiftCommonCrypto', '~> 0.13.0'
	pod 'SwiftLint'

	target 'StraalTests' do
		inherit! :search_paths
		pod 'Quick'
		pod 'Nimble'
	end
end
